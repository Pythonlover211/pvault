{
  "name": "pvault",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --test-isolation=none",
    "dev": "node scripts/dev-server.js"
  },
  "engines": { "node": ">=20" }
}
